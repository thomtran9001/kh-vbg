config {
  type: "table",
  uniqueKey: ['id'],
  schema: "analytic_service",
  bigquery: {
    labels: {
      workflow: "analytic_service"
    }
  }
}

SELECT DISTINCT * FROM ${ref('raw_gia_chao')}