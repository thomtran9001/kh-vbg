config {
  type: "table",
  uniqueKey: ['id'],
  schema: "analytic_wework",
  bigquery: {
    labels: {
      workflow: "analytic_wework"
    }
  }
}

SELECT DISTINCT * FROM ${ref('raw_ke_hoach_quy')}